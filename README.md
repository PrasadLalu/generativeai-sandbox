# Generativeai Sandbox

## Create Environment:
```
1. Create:
python -m venv myenv

// Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
2. Activate:
.\myenv\Scripts\Activate.ps1
```